package com.example.stonks;

public class OutputMovement extends Movement{
    public OutputMovement(String description, boolean isDebt, double mount) {
        super(description, isDebt, mount);
    }
}
